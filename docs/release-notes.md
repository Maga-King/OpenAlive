修复清空应用数据或重启后，新增时钟提示“系统尚未加载新版时钟模块”的问题。ColorOS 会拦截 SystemUI 拉起 OpenAlive 的时钟服务；现在打开 OpenAlive 时会通过系统的关联启动接口完成设置，并核对结果。时钟服务暂时不可用时也会自动重试。

还改进了 SystemUI 首次加载、解锁前后恢复和编辑器的错误提示。需要 Root 与 LSPosed；更新后请正常重启并解锁一次。

APK 由 GitHub Actions 从本次源码构建。部分资产来源于魅族 22 FlymeOS，另含澎湃系统时钟资源；自实现代码采用 MIT 许可证，官方素材保留原有权利。
